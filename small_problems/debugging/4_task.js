// should use the shift method instead of the delete operator
// the delete operator removes properties from an object; when used with an array element,
// it leaves behind an empty item

// should get used to using splice more, it is extremely useful